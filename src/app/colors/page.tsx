import styles from './colors.module.css';

export default function Colors() {
  return (
    <div className={styles.page}>
      this is coming soon, but ideally it will show what colors we have available to print with.
    </div>
  );
}